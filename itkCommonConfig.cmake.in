@PACKAGE_INIT@

set(PATH_TARGET "${CMAKE_CURRENT_LIST_DIR}/ITKCommonTargets.cmake")

message("PATH_TARGET = ${PATH_TARGET}")

include(${PATH_TARGET})

get_target_properties(CHAUD ITKCommon::ITKCommon INTERFACE_INCLUDE_DIRECTORIES)

message("CHAUD = ${CHAUD}")